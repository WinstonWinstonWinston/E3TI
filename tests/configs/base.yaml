# configs/config.yaml  (top level)
defaults:
  - module: base
  - data: ADP_dataset
  - experiment: train
  - paths: default
  - _self_

num_device: 1
seed: 69420

project:
  name: E3TensorInterpolants
