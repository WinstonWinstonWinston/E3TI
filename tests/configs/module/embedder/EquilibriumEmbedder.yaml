_target_: e3ti.model.embedding.equilibrium_embedder.EquilibriumEmbedder
use_ff: True

interp_time:
    embedding_dim: 64
    max_positions: 1000

force_field: 
    in_dim: 4
    hidden_dims: [128, 64]
    out_dim: 32
    activation: relu
    use_input_bn: True
    affine: True
    track_running_stats: True

atom_type: 
    num_types: 10
    embedding_dim: 32